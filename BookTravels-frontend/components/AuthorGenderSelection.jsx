import React from "react";
import { StyleSheet, Text, View } from "react-native";

const AuthorGenderSelection = ({}) => {
  return (
    <View style={styles.container}>
      {/* options: female, male, non-binary, other, unknown */}
    </View>
  );
};

export default AuthorGenderSelection;

const styles = StyleSheet.create({
  container: {},
});
